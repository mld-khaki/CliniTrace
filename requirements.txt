# Runtime dependencies for CliniTrace on Streamlit Community Cloud.
#
# Streamlit Cloud reads requirements.txt as the most reliable dependency
# signal. We KEEP pyproject.toml as the canonical source for local dev
# (uv / pip install -e .), and this file mirrors the runtime + gui
# extras only — no dev tooling (pytest, ruff, hypothesis) so the cloud
# build is small and fast.
#
# When pyproject.toml dependencies change, regenerate this file:
#   uv export --no-dev --extra gui --format requirements-txt > requirements.txt
#
# Core data-pipeline dependencies (from pyproject.toml [project])
pydantic>=2.6,<3
pandas>=2.1
pyarrow>=15
pyyaml>=6

# GUI dependencies (from pyproject.toml [project.optional-dependencies.gui])
streamlit>=1.36
streamlit-option-menu>=0.3.6
